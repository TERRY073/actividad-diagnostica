# 🚀 Actividad: Saberes Previos - HTML y CSS

## 🎯 Objetivo

Validar los conocimientos fundamentales de **HTML semántico** y **CSS (incluyendo Responsive Design)** mediante la construcción de un formulario.

Adicionalmente, se evaluará la comprensión del flujo de trabajo básico de Git (Fork, Clonar, Confirmar cambios y Pull Request).

## 📋 Escenario

Debes construir un formulario de registro o contacto. El diseño es completamente libre (**"a libre diseño"**), pero debe ser funcional, accesible y visualmente agradable.

## 🛠️ Requisitos Técnicos

### HTML (Estructura y Semántica)

Tu archivo `.html` debe incluir:
* Un tag `<form>`.
* Uso correcto de `<label>` asociado a cada `input`.
* Al menos **4 tipos de inputs** diferentes (ej. texto, email, contraseña, checkbox, radio, fecha, etc.).
* Un `<button>` de tipo `submit` para enviar el formulario.
* El archivo HTML debe estar enlazado correctamente al archivo CSS.

### CSS (Estilos y Responsive Design)

Tu archivo `.css` debe demostrar:
* **Estilos personalizados:** El formulario NO debe tener los estilos por defecto del navegador.
* **Responsive Design:** El formulario debe visualizarse correctamente tanto en dispositivos móviles como en escritorio.
* Uso adecuado de selectores (clases, IDs) para aplicar estilos.

---

## 📂 Nomenclatura de Archivos

Para evitar conflictos, **no debes crear carpetas**.

Debes nombrar tus archivos usando tu nombre o usuario. Por ejemplo, si tu nombre es `JaimeZapata`:

* Tu archivo HTML se llamará: `JaimeZapata.html`
* Tu archivo CSS se llamará: `JaimeZapata.css`

(Asegúrate de enlazar correctamente el CSS dentro del HTML).

---

## ⚙️ Flujo de Trabajo y Envío (Git)

1.  **Fork:** Realiza un Fork de este repositorio a tu cuenta personal.
2.  **Clonar:** Clona tu fork (la copia en tu cuenta) a tu computador local.
3.  **Desarrollar:** Crea los dos archivos (`.html` y `.css`) con tu nombre en la raíz del proyecto y desarrolla la solución.
4.  **Confirmar (Commit):** Guarda tus cambios localmente.
5.  **Subir (Push):** Sube los cambios a tu fork en GitHub.
6.  **Enviar (Pull Request):** Abre un Pull Request (PR) desde tu fork hacia el repositorio original para la revisión.